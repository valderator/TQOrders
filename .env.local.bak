# Copy to .env.local (never commit real keys).
# Supabase → Project Settings → API
# Leaving these empty runs the app in offline "local mode" with demo accounts.
# URL must be the bare project URL - no /rest/v1, no trailing slash.
EXPO_PUBLIC_SUPABASE_URL=https://zxkvzfcogpdsoxlpbbky.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_8wlhXIGvOZgxW5SScg454g_N4l3GWxE
